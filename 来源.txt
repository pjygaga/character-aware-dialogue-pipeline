https://www.kaggle.com/datasets/maricinnamon/harry-potter-movies-dataset
github.com/gastonstat/harry-potter-datas

ai:
做dataset筛选 筛列数 排数这些
gitignore他写的

scripts\prepare_datasets.py他写的def print_distribution(name, dataset, label_key="label"):


We used the GPT-4o-mini API to generate initial emotion labels for our 600-sample dataset, following the annotation guidelines we developed (see docs/annotation_guidelines.md). We iterated on the prompt across three versions, with manual accuracy checks on a 50-sample validation set at each stage, improving accuracy 
# Emotion Labeling: Iteration Log

## v1: Initial Labeling
- System: GPT-4o-mini, temperature=0
- Guidelines: annotation_guidelines_v1.md (1500 tokens)
time: 5:50 minutes
- Label distribution: neutral 39.2%, worried 23.8%, determined 20.5%, angry 10.3%, happy 3.3%, sad 2.8%

## Observation
Manual review of 100 random samples (stratified by label) showed 87% accuracy. Key issue: 'neutral' class was over-predicted, likely due to overly permissive definition in guidelines. Mis-labeled 'neutral' samples often belonged to mild 'determined' or 'worried' categories.

## v2: Refined Guidelines
Changes:
1. Tightened 'neutral' definition to exclude any detectable emotional tone
2. Added disambiguation rules for 'angry' vs 'worried'
3. Added 2 more examples per class for 'happy' and 'sad'

Re-labeled same 100 samples with v2 guidelines. Accuracy improved to 93/100.

## Decision
Re-ran all 600 lines with v2 guidelines. Final dataset saved to data/processed/hermione_labeled_v2.json.



这里有个坑你要看见
你仔细看 sad 在三个集合里的数量：

Train: 13 条
Val: 1 条
Test: 3 条

val 里只有 1 条 sad。这意味着什么？
分类器训练完以后，如果你在 val 上测准确率，sad 这个类别的数据就只有 1 条。预测对了是 100%，错了是 0%，根本不是一个有意义的数字。
这是你数据本身导致的问题，不是代码错。但你必须在报告里写出来，比如这样：

由于 sad 类别原始样本仅 17 条，切分后 val 集只有 1 条，test 集只有 3 条，此类别的评估结果统计意义有限。这是赫敏角色的固有特征导致的（坚强角色悲伤台词少）。我在后续分类器训练中将使用加权损失（weighted loss）缓解此问题。

这就是 rubric 里的 技术决策记录。


这也太多了 简短

18:37
Claude responded: Baseline phase: ran 30 test questions across three prompt versions (zero-shot A, detailed instruction B, few-shot with real movie lines C) on gpt-4o-mini, 90 r…
Baseline phase: ran 30 test questions across three prompt versions (zero-shot A, detailed instruction B, few-shot with real movie lines C) on gpt-4o-mini, 90 responses total. Your intuition is right, A and B still sound like a chatbot in costume, C actually sounds like a character.

The clearest evidence is formatting. B writes bold markdown and numbered lists when asked things like how to prep for OWLs, which real Hermione would never do, she would snap at you. This means the underlying model still thinks it is answering a query, not reacting as a friend. C has zero bold and zero lists across all 30 responses.

Length is striking once you quantify it. A averages 95 words, B 113, C only 48. C is half the length and that is exactly why it feels human, Hermione snaps things like "Are you completely mad? Skip Potions?" instead of lecturing for 80 words.

Something weird with B, it has "Honestly" in all 30 responses and "I read" in 25, because the prompt literally told it to use those words. It now overdoes them to the point of parody. C uses "Honestly" 11 times which is more natural, and says "I read" only once but still feels more like Hermione. So telling the model what words to use is worse than showing it examples, because examples carry rhythm and context not just vocabulary.

Category matters a lot. On academic questions all three are ok because lecturing fits. The gap explodes on friendship and conflict questions (Q3, Q11, Q17), where A and B give polite assistant advice and C actually snaps back like a friend. For the final report the best comparison is a conflict question not an academic one.

Going into LoRA, the real baseline to beat is C not A. What C still misses is the very short clipped responses (real Hermione often has 4-15 word lines in the dataset) and specific book citations with chapter numbers, C makes up generic sounding titles. LoRA should hopefully fix both.




Realized an important limitation of LoRA fine-tuning. LoRA on dialogue data only teaches the model how Hermione speaks, not what she knows. The world knowledge about Harry Potter universe (spell mechanics, character history, SPEW details) comes entirely from Phi-2 base model, and LoRA cannot add new knowledge, only restyle existing outputs. About 40% of my 30 test questions require world knowledge (Q1 wand movement, Q21 SPEW, Q26 mudblood context), and on these the LoRA output will depend heavily on Phi-2's prior knowledge which is limited and sometimes wrong. Going to keep these questions as stress tests for error analysis section, since they reveal the knowledge-style separation that is a known characteristic of style-focused fine-tuning.


01——hermione_ lora training

#AI generated

from datasets import Dataset

#AI generated

def format_sample(sample):
    # Combine instruction, input, and output into a single training text
    if sample.get("input"):
        text = (
            f"### Instruction:\n{sample['instruction']}\n\n"
            f"### Input:\n{sample['input']}\n\n"
            f"### Response:\n{sample['output']}"
        )
    else:
        text = (
            f"### Instruction:\n{sample['instruction']}\n\n"
            f"### Response:\n{sample['output']}"
        )
    return {"text": text}

# Sample 100 for sanity check
import random
random.seed(42)
sanity_data = random.sample(data, 100)

# Convert to Hugging Face Dataset
sanity_dataset = Dataset.from_list([format_sample(s) for s in sanity_data])

print("First formatted training text:")

print(sanity_dataset[0]['text'])


Key Insight from Phase 3.2
Issue detected: Initial loss started at 5.16 (too high) because labels included the instruction/input template text, forcing the model to waste capacity memorizing fixed prompt tokens instead of learning Hermione's response style.
Fix applied: Added loss masking to set prefix tokens and padding tokens to -100, so the model only computes loss on the ### Response: content, concentrating all 5.2M trainable LoRA parameters on learning the actual dialogue style.
Expected improvement: Loss curve now starts 3.8 and converges more meaningfully, giving Phase 3.3's full training a much cleaner signal and better final model quality.


3.3的格式化 + tokenize是ai生成的 因为复用了步骤2的基本一致 所以让他帮我直接改了
# Check active token distribution across samples也是ai生成代码 
# Build a comparison table ai生成
# Plot training loss curves ai帮助
# Build comparison table ai

"Response sequences are short (average ~10 tokens), which limits the signal per sample but reflects authentic movie dialogue patterns."


version 1：The Phi-2 + LoRA model shows clear signs of learning Hermione's linguistic patterns 
(imperative voice, emphasis on studying and rules, lexical patterns like "you've got to", 
"lots and lots of"), but also exhibits common small-model issues: occasional repetition 
loops, factual hallucination (fictional details not in canon), and incomplete sentences.

These limitations reflect:
1. Small base model (Phi-2 at 2.7B vs. hundreds of billions for SOTA models)
2. Limited training data (537 samples; industry recipes use 10K-100K)
3. PEFT-LoRA only modifies 0.19% of parameters

These are documented as expected tradeoffs of the chosen approach, not execution failures.


## Phase 3.4 Evaluation Findings

The fine-tuned LoRA model successfully captures several of Hermione Granger's 
linguistic patterns: imperative voice ("You have to study, Harry!"), emphasis 
on rules and study, and protective concern for friends. Emotion conditioning 
is observable: SAD-conditioned outputs use longing language ("Where did all 
the joy go..."), while WORRIED outputs emphasize protective vigilance.

However, the model exhibits notable limitations:

1. **Factual hallucinations**: The model invents non-canon details, including 
   fictional characters ("Lily Lovegood"), incorrect spell effects 
   ("Wingardium Leviosa lets you fly"), and historical errors. Out of 30 
   test responses, ~15 contained at least one factual deviation.

2. **Character role confusion**: In some responses, the model attributes 
   Hermione's voice to other characters or has Hermione speak to wrong 
   conversational partners (e.g., responding to Hagrid when no Hagrid 
   was mentioned in the prompt).

3. **Missing signature phrases**: Despite training, the model rarely produces 
   Hermione's most iconic phrases ("Honestly, Ronald," references to 
   "Hogwarts: A History"). This likely reflects scarcity in training data.

These limitations are consistent with the chosen approach's known constraints:
small base model (Phi-2 at 2.7B parameters), limited fine-tuning data 
(537 samples), and PEFT-LoRA modifying only 0.19% of parameters. They are 
documented as expected tradeoffs, not execution failures.



在3.5部分 100 diverse prompts for generating Hermione responses这个用了ai的帮助 自己写了四十条
分析标注结果（distribution shift 分析）是ai帮写的


3.5结束后：### Distribution Shift Analysis (Phase 3.5)

To assess the generalization gap between the original Hermione dialogue 
and the LoRA model's outputs, we generated 100 responses with explicit 
emotion conditioning, then re-classified them with GPT-4o-mini using the 
identical annotation guidelines applied to the training data.

**Findings**:

1. **Overall intent-output mismatch (41% match rate)**: Across 100 samples, 
   only 41% had a predicted emotion matching the intended conditioning. 
   While 2.5x above random baseline (16.7%), this confirms that LoRA's 
   emotion control on Phi-2 is partial.

2. **"Worried" bias** (41 predicted vs 18 intended, +23): The model 
   over-produces hedging language ("What if...", "I'm not sure...") 
   regardless of intended emotion. Likely due to:
   - Imbalanced training data (worried = 24% of samples)
   - Phi-2's pretrained tendency toward cautious/uncertain phrasing
   
3. **"Angry" near-collapse (12.5% match rate)**: Of 16 intended-angry 
   prompts, 7 were classified as worried, 5 as determined, only 2 as angry. 
   Suggests LoRA failed to learn Hermione's anger patterns. Likely due to 
   insufficient angry training samples (56 of 537 = 10.4%).

**Implications for Phase 4**: This distribution shift sets up a meaningful 
test for the emotion classifier — the v1 classifier (trained on original 
movie dialogue) is expected to underperform on LoRA-generated text, and 
augmenting v3's training set with these 100 labeled samples should improve 
performance on this distribution.


阶段4导入文件部分的函数用了ai
Quick sanity check: compare with v1 predictions to compute "agreement" 用了ai
def plot_training_curves(version_names):用了ai


at phase 4 Even with fixed random seed, run-to-run variance was ~0.03 macro-F1, suggesting the model is sensitive to initialization on this small dataset. Multiple runs and averaging would give more reliable estimates.

早停在两个崩盘run里都触发了
仔细看：
dropout实际跑了几个epoch早停触发？0.18/8（跑满）没触发，还在改善0.25/8就停了触发了，val F1连续2轮下降0.34/8就停了触发了，val F1崩了

第一部分：基本设置写什么：模型选了什么、为什么、数据集多大、有什么特点。可用的事实：
选 bert-base-uncased，因为这是Lecture 10里讲过的标准微调起点，且参数量适中（4090或Colab T4都能跑）
训练集478条、验证集58条、测试集64条——用80/10/10分层抽样
6类情绪：determined, worried, angry, happy, sad, neutral
类别非常不平衡：neutral 184条 vs sad 13条（差14倍）
应对策略：class-weighted cross-entropy，权重和类别频率成反比（neutral权重0.49，sad权重6.02）
可以加一句的反思：sad只有13条，无论怎么调都难训——这个局限性要承认。第二部分：v1（第一次训练）写什么：用什么数据、训练完什么效果、暴露了什么问题。可用的事实：
用478条原作台词训练
超参数：lr=2e-5，batch_size=16，dropout=0.1，weight_decay=0.01，warmup=10%，最多8 epochs
用val macro F1做early stopping，patience=2
v1结果：accuracy 0.50，macro F1 0.410，weighted F1 0.484
各类F1：neutral 0.760, happy 0.667, angry 0.400, determined 0.381, worried 0.250, sad 0.000
可以观察的现象：

v1对多数类（neutral）表现很好（F1=0.76），少数类（sad）完全失败（F1=0）
worried虽然训练样本不算少（115条），但F1只有0.25，说明这一类边界不清
happy只有18条训练数据，但因为class weighting，F1=0.667比预期高
第三部分：诊断（v1暴露了什么）写什么：让v1去预测100条LoRA文本，看它的反应。可用的事实：
100条LoRA生成文本（API标注的"真值"分布）：worried 41, determined 26, neutral 13, happy 12, angry 5, sad 3
v1对这100条的预测分布：worried 49, determined 24, angry 10, happy 10, neutral 6, sad 1
v1的预测平均置信度只有0.375（6类随机猜是0.167，所以高于随机但远谈不上自信）
v1与API标签一致率65%（也就是100条里35条判错）
关键观察：

v1过度预测worried（49 vs 真值41）和过度预测angry（10 vs 真值5）
v1漏掉了一半neutral（预测6 vs 真值13）
v1几乎没识别出sad（只判出1条 vs 真值3条）
低置信度案例的例子（你贴过的）：

"I'm sorry, I just wanted to see them up close..." 判sad p=0.22
"You did it? You killed your own father!" 判angry p=0.24
这些案例说明v1对LoRA生成的文本"语感不熟"。第四部分：v2（第二次训练）写什么：怎么改进、为什么改、效果如何。可用的事实：
把100条LoRA文本（带API情绪标签）加进训练集，变成578条
重新计算class weights（因为分布变了一些）
其他超参数和v1完全一样——这样任何提升都来自数据，不是参数变化
v2结果：accuracy 0.547，macro F1 0.453，weighted F1 0.546
各类F1的变化（这是最有说服力的部分）：类别v1v2变化worried0.2500.500+0.250（最大提升）angry0.4000.500+0.100determined0.3810.421+0.040sad0.0000.0000（依然失败）neutral0.7600.727-0.033（略降）happy0.6670.571-0.096（略降但样本太少）可以解释的因果关系：

LoRA增强数据里worried有41条，最多——v2 worried提升+25个点最大
LoRA数据里sad只有3条——v2 sad依然是0
所以"哪一类的增强数据多 → 哪一类提升大"，关系很清楚
可以反思的点：

happy和neutral略降可能是噪声（这两类增强数据较少）
也可能是因为classifier head随机初始化的run-to-run variance（约0.03）
多跑几次取平均才更可靠，但时间不够
第五部分：调参实验（dropout）写什么：试了几个dropout，哪个最好，发现了什么有趣的现象。可用的事实：
用augmented数据集试了 dropout = {0.1, 0.2, 0.3}
其他超参数不变
dropoutmacro F1状态0.10.424最好0.20.315提前early stop（5/8 epoch）0.30.128严重崩盘（4/8 epoch停）dropout=0.3的崩盘细节（值得讲）：

模型几乎把所有东西都判成worried
determined、happy、sad三类F1都是0
neutral 23条只对3条
这就是Lecture 05讲的"dropout过强导致underfit"
可以提的reasoning：

数据集只有578条，每epoch只有约36个batch
dropout=0.3意味着每次30%神经元被屏蔽，noise太大
早停机制成功识别了崩盘并提前终止了训练
dropout=0.1和单独的v2虽然都是dropout=0.1但F1不同（0.424 vs 0.453）的解释：

这是run-to-run variance
即使seed固定，HuggingFace Trainer的某些随机性没被完全控制
0.03的波动在小数据集小模型上是正常的
第六部分：最终模型选择和局限性写什么：选了哪个模型当最终版、有什么没解决的问题。可用的事实：
最终模型选 v2（dropout=0.1，augmented data，macro F1 = 0.453）
调参实验确认了dropout=0.1是合理选择
诚实交代的局限性：
sad类彻底失败：训练集只有13条，class weighting把单条sad的惩罚放大了6倍仍学不到稳定特征——本质是数据问题，不是模型问题
happy/sad测试集太小：分别只有3条，单条对错决定整类F1，统计意义有限
没做cross-validation：时间不够，单次train/test split可能有方差
API标签也不是gold standard：API标的100条本身可能有错，但和stage 1保持一致是为了标注一致性
run variance：相同设置不同run F1差0.03，说明结果应该被理解为"某个范围"而不是某个精确值
把这些素材组织成日志的建议结构上不要分"v1 / v2 / hyperparam"三段平铺，那样太流水账。建议这样讲故事：
背景（1段）：什么任务、为什么是难的（数据少、不平衡）、对策是什么
第一次尝试（1-2段）：v1长什么样、表现如何
发现问题（1段）：诊断的100条数据告诉了我们什么
第二次尝试（1-2段）：v2怎么改、效果如何、最大的提升在哪
调参验证（1段）：dropout三个值、为什么0.3崩了、印证了什么
没解决的事（1段）：sad、small test set、variance




in 03 pipeline
load trained lora and bert model 用了ai
!pip install -q compel compel包是ai教的


SD 1.5 在小脸构图下面部细节不稳定。我们尝试了 5 版 prompt 调整：v1 缺乏情绪区分；v2 简化 prompt 后情绪表达太弱；v3 加入肖像构图改善画质但情绪仍模糊；v4 用 Compel 权重语法 (++)+++ 强化情绪后表情清晰但身份漂移（人种、年龄、发型不一致）；v5 给身份特征同样加权后达到平衡，但仍有微妙的"诡异感"。这反映了 SD 1.5 在小模型容量下情绪与身份特征的注意力竞争。未来工作可以换用 SDXL 或加 face restoration 后处理。