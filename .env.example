# Copy this file to .env and fill in real values.
# .env is gitignored — never commit real keys.

# OpenAI API key (used by src/data/label_emotions.py for Stage 1
# emotion labeling and src/baseline/run_baseline.py for the Stage 2
# baseline. Notebook 01 also uses it for the LoRA distribution-shift
# re-labeling in Phase 3.5.)
OPENAI_API_KEY=
